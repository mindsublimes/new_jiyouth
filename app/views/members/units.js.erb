<% if @units.count > 0 %>
	$('#area_unit').html("<%= escape_javascript(render partial: 'units', locals: { units: @units } ) %>")
<% else %>
	$('#area_unit').html("<%= escape_javascript(render partial: 'empty_partial', locals: { units: @units } ) %>")
<% end %>